apiVersion: v1
kind: Service
metadata:
  name: service-devops-tech-challenge
  namespace: dev
spec:
  type: NodePort
  selector:
    app: devops-tech-challenge
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080

